define(
	[
		'angular'
	],
	function(angular) {
		mod_services.service('uploadService',['cService', '$parse', function( cService, $parse ) {
            var
                service;
            service = {
               
            }
			return service;

		} ] )
	}
);