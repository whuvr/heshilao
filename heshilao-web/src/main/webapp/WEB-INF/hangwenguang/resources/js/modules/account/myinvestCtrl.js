define([
    'angular'
], function(
    angular
) {
    return function(app, elem, attrs, scope) {
        app.controller('myinvestCtrl', ['$scope','cService', '$rootScope','$timeout','popupDialog',function( $scope,cService,$rootScope,$timeout,popupDialog) {
            'use strict';
           
                   
        }])
    }
});