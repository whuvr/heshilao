define([
    'angular',
    'services/ui/dialogService'
], function(
    angular,
    dialogService
) {
    return function(app, elem, attrs, scope) {
        app.controller('creditorRightCtrl', [
            '$scope',
            function( $scope) {
        	'use strict';


            
        }])
    }
});